pipeline {
    agent {
        label '(build-in)'
}
    stages {               // conjunto de etapas
        stage('Nome') {    // uma etapa específica
            steps {        // ações dentro da etapa
                sh 'cat "/var/jenkins_home/workspace/Teste 002/artefatos/build-14.txt"'
            }
        }
    }
}
